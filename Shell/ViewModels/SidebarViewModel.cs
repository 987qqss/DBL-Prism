namespace Shell.ViewModels
{
    /// <summary>
    /// 侧栏容器 ViewModel —— 已简化为空壳。
    /// 原树形逻辑已迁移至 DeviceModule.DeviceCommandTreeViewModel。
    /// SidebarView 不再使用 AutoWireViewModel，仅作为 Region 容器。
    /// </summary>
    public class SidebarViewModel
    {
    }
}
