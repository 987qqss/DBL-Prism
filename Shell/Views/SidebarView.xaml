<UserControl x:Class="Shell.Views.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="{StaticResource SidebarBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 顶部标题 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="12,16,12,12">
            <TextBlock Text="☰" FontSize="18" Foreground="{StaticResource AccentCyanBrush}" VerticalAlignment="Center" />
            <TextBlock Text="设备命令树"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       FontSize="14"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="8,0,0,0" />
        </StackPanel>

        <!-- 中间区域：Prism Region，由 DeviceModule 注入 DeviceCommandTreeView -->
        <ContentControl Grid.Row="1"
                        prism:RegionManager.RegionName="SidebarTreeRegion"
                        Margin="0" />

        <!-- 底部：版本号 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="12,8,12,12" HorizontalAlignment="Center">
            <Ellipse Width="8" Height="8" Fill="#22C55E" VerticalAlignment="Center" />
            <TextBlock Text="v1.0.0  |  Prism + WPF"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="11"
                       VerticalAlignment="Center"
                       Margin="6,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
