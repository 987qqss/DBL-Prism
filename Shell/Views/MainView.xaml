<Window x:Class="Shell.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        Title="储能集装箱电池仓数据监测系统" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#2C3E50" Margin="10">
            <Label Content="设备监控系统" Foreground="White" FontSize="16" FontWeight="Bold"/>
            <Separator Width="2" Background="White" Margin="10,0,10,0"/>
            <Button Content="设备状态" Command="{Binding ShowDeviceViewCommand}" Margin="5" Padding="10,5"/>
            <Button Content="报警管理" Command="{Binding ShowAlarmViewCommand}" Margin="5" Padding="10,5"/>
            <Button Content="操作管理" Command="{Binding ShowOperationViewCommand}" Margin="5" Padding="10,5"/>
        </StackPanel>
        
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="ContentRegion"/>
        
        <StatusBar Grid.Row="2" Background="#ECF0F1">
            <StatusBarItem>
                <TextBlock Text="就绪"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>