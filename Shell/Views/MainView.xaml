<Window x:Class="Shell.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:Shell.ViewModels"
        xmlns:views="clr-namespace:Shell.Views"
        xmlns:viewsControls="clr-namespace:Shell.Views.Controls"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=False}"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="800" Width="1400"
        MinHeight="600" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource ContentBackground}"
        WindowStyle="None"
        AllowsTransparency="True"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      GlassFrameThickness="0"
                      ResizeBorderThickness="6" />
    </WindowChrome.WindowChrome>

    <Border BorderBrush="{StaticResource BorderBrushDark}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />   <!-- Row 0: 标题栏 -->
                <RowDefinition Height="36" />   <!-- Row 1: 菜单栏（缩小高度） -->
                <RowDefinition Height="*" />    <!-- Row 2: 内容区 -->
                <RowDefinition Height="28" />   <!-- Row 3: 状态栏 -->
            </Grid.RowDefinitions>

            <!-- ============ Row 0: 标题栏 ============ -->
            <Border Grid.Row="0" Background="{StaticResource TitleBarBackground}">
                <Grid Height="32">
                    <!-- 系统名称 -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                        <materialDesign:PackIcon Kind="Flash"
                                                 Foreground="{StaticResource AccentCyanBrush}"
                                                 Width="16" Height="16"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="  储能集装箱电池仓数据监测系统"
                                   FontFamily="Segoe UI"
                                   FontSize="12"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource AccentCyanBrush}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- 窗口控制按钮 -->
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,2,4,2">
                        <!-- 最小化按钮 -->
                        <Button x:Name="BtnMinimize"
                                Style="{StaticResource TitleBarButtonStyle}"
                                WindowChrome.IsHitTestVisibleInChrome="True"
                                Command="{Binding MinimizeCommand}"
                                ToolTip="最小化">
                            <Path Width="10" Height="10" 
                                  Fill="{Binding Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}"
                                  Data="M0,5 L10,5"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}"
                                  StrokeThickness="1.5" />
                        </Button>
                        <!-- 最大化按钮 -->
                        <Button x:Name="BtnMaximize"
                                Style="{StaticResource TitleBarButtonStyle}"
                                WindowChrome.IsHitTestVisibleInChrome="True"
                                Command="{Binding MaximizeCommand}"
                                ToolTip="最大化">
                            <Path Width="10" Height="10" 
                                  Fill="Transparent"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}"
                                  StrokeThickness="1.5"
                                  Data="M1,1 L9,1 L9,9 L1,9 Z" />
                        </Button>
                        <!-- 关闭按钮 -->
                        <Button x:Name="BtnClose"
                                Style="{StaticResource CloseButtonStyle}"
                                WindowChrome.IsHitTestVisibleInChrome="True"
                                Command="{Binding CloseCommand}"
                                ToolTip="关闭">
                            <Path Width="10" Height="10" 
                                  Fill="{Binding Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}"
                                  Data="M1,1 L9,9 M9,1 L1,9"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}"
                                  StrokeThickness="1.5" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ============ Row 1: 菜单栏 ============ -->
            <views:MenuBarView x:Name="MenuBar" Grid.Row="1" />

            <!-- ============ Row 2: 内容区 (左侧栏 + 中间 Prism 区域) ============ -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <!-- 侧栏（可折叠） -->
                    <ColumnDefinition Width="Auto" />
                    <!-- 主内容 -->
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- 侧栏导航（可折叠） -->
                <Border x:Name="SidebarBorder"
                        Grid.Column="0"
                        Background="{StaticResource SidebarBackground}"
                        BorderBrush="{StaticResource BorderBrushDark}"
                        BorderThickness="0,0,1,0"
                        Width="200">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.IsSidebarOpen, ElementName=MenuBar}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <views:SidebarView />
                </Border>

                <!-- 主内容区 - Prism Region -->
                <ContentControl Grid.Column="1"
                                prism:RegionManager.RegionName="ContentRegion"
                                Margin="0">
                    <ContentControl.Template>
                        <ControlTemplate TargetType="ContentControl">
                            <Grid Background="{StaticResource ContentBackground}">
                                <ContentPresenter />
                            </Grid>
                        </ControlTemplate>
                    </ContentControl.Template>
                </ContentControl>
            </Grid>

            <!-- ============ Row 3: 状态栏 ============ -->
            <Border Grid.Row="3" Background="{StaticResource StatusBarBackground}" Padding="12,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="Circle"
                                                 Width="8" Height="8"
                                                 Foreground="#22C55E"
                                                 VerticalAlignment="Center" />
                        <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="{StaticResource AccentCyanBrush}"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   Margin="8,0,0,0" />
                    </StackPanel>

                    <TextBlock Grid.Column="1"
                               Text="{Binding CurrentTime}"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               FontSize="11"
                               VerticalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
