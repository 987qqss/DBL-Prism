<UserControl x:Class="Shell.Views.Controls.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Shell.Views.Controls"
             Background="Transparent">

    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="280" />
        </Grid.ColumnDefinitions>

        <!-- 左侧：上(命令执行) + 下(日志) -->
        <Grid Grid.Column="0" Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="280" />
            </Grid.RowDefinitions>

            <views:CommandRunView Grid.Row="0" Margin="0,0,0,8" />
            <views:LogView Grid.Row="1" />
        </Grid>

        <!-- 右侧：设备状态 + 参数设置 -->
        <views:StatusParamView Grid.Column="1" />
    </Grid>
</UserControl>
