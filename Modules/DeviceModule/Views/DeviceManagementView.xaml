<UserControl x:Class="DeviceModule.Views.DeviceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:vm="clr-namespace:DeviceModule.ViewModels"
             xmlns:views="clr-namespace:DeviceModule.Views"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#0F1626">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <views:DeviceTreeView Grid.Column="0" />
        
        <Border Grid.Column="1" BorderBrush="#2A2A4A" BorderThickness="1,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" Background="#1E1E2E" Padding="16">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="设备配置" FontSize="18" FontWeight="Bold" Foreground="#E2E8F0" />
                    </StackPanel>
                </Border>
                
                <ContentControl Grid.Row="1" prism:RegionManager.RegionName="DeviceConfigRegion" />
            </Grid>
        </Border>
    </Grid>
</UserControl>