<UserControl x:Class="UserModule.Views.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#0F1626">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E2E" BorderBrush="#313244" BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="用户管理" FontSize="20" FontWeight="Bold" Foreground="#E2E8F0" Margin="0,0,30,0"/>
                
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="当前用户: " Foreground="#94A3B8"/>
                    <TextBlock Text="{Binding CurrentUser}" Foreground="#FBBF24" FontWeight="Bold"/>
                </StackPanel>

                <Button Content="添加用户" Command="{Binding AddUserCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}" Margin="30,0,10,0"/>
                <Button Content="修改密码" Command="{Binding ModifyPasswordCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="退出登录" Command="{Binding LogoutCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}"
                        Background="#EF4444" Foreground="White"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="0,15,0,0">
            <DataGrid ItemsSource="{Binding Users}" 
                      AutoGenerateColumns="False"
                      Background="#1E1E2E"
                      BorderBrush="#313244"
                      RowBackground="#1E1E2E"
                      AlternatingRowBackground="#252536"
                      Foreground="#E2E8F0"
                      BorderThickness="1">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#252536"/>
                            <Setter Property="Foreground" Value="#94A3B8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="用户名" Binding="{Binding UserName}" Width="150"/>
                    <DataGridTextColumn Header="角色" Binding="{Binding Role}" Width="120"/>
                    <DataGridTemplateColumn Header="状态" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="10" Height="10" 
                                             Fill="{Binding Status, Converter={StaticResource StatusToBrushConverter}}" 
                                             Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding Status}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>