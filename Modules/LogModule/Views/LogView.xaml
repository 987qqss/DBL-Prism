<UserControl x:Class="LogModule.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:LogModule.Converters"
             Background="#0F1626">
    <UserControl.Resources>
        <converters:LogLevelToBrushConverter x:Key="LogLevelToBrushConverter" />
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E2E" BorderBrush="#313244" BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="日志管理" FontSize="20" FontWeight="Bold" Foreground="#E2E8F0" Margin="0,0,30,0"/>
                
                <TextBlock Text="级别:" Foreground="#94A3B8" Margin="0,0,8,0"/>
                <ComboBox SelectedItem="{Binding SelectedLevel}" Width="100" Background="#252536" Foreground="White" BorderBrush="#313244">
                    <ComboBoxItem>全部</ComboBoxItem>
                    <ComboBoxItem>Debug</ComboBoxItem>
                    <ComboBoxItem>Info</ComboBoxItem>
                    <ComboBoxItem>Warn</ComboBoxItem>
                    <ComboBoxItem>Error</ComboBoxItem>
                </ComboBox>

                <TextBlock Text="关键词:" Foreground="#94A3B8" Margin="20,0,8,0"/>
                <TextBox Text="{Binding SearchKeyword}" Width="200" Background="#252536" Foreground="White" BorderBrush="#313244"/>

                <Button Content="导出日志" Command="{Binding ExportLogCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}" Margin="20,0,10,0"/>
                <Button Content="清空日志" Command="{Binding ClearLogCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}"
                        Background="#EF4444" Foreground="White"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="0,15,0,0" VerticalScrollBarVisibility="Auto">
            <ListBox ItemsSource="{Binding Logs}" Background="Transparent" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#1E1E2E" BorderBrush="#313244" BorderThickness="0,0,0,1" Padding="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Time}" Foreground="#64748B" FontFamily="Consolas"/>
                                <TextBlock Grid.Column="1" Text="{Binding Level}" 
                                           Foreground="{Binding Level, Converter={StaticResource LogLevelToBrushConverter}}"
                                           FontWeight="Bold"/>
                                <TextBlock Grid.Column="2" Text="{Binding Source}" Foreground="#94A3B8"/>
                                <TextBlock Grid.Column="3" Text="{Binding Message}" Foreground="#E2E8F0" TextWrapping="Wrap"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</UserControl>
