<UserControl x:Class="LogModule.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#0F1626">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E2E" BorderBrush="#313244" BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="日志管理" FontSize="20" FontWeight="Bold" Foreground="#E2E8F0" Margin="0,0,30,0"/>
                
                <TextBlock Text="日志级别:" Foreground="#94A3B8" Margin="0,0,8,0"/>
                <ComboBox SelectedItem="{Binding LogLevel}" Width="100" Background="#252536" Foreground="White" BorderBrush="#313244">
                    <ComboBoxItem>全部</ComboBoxItem>
                    <ComboBoxItem>INFO</ComboBoxItem>
                    <ComboBoxItem>WARN</ComboBoxItem>
                    <ComboBoxItem>ERROR</ComboBoxItem>
                </ComboBox>

                <TextBlock Text="关键词搜索:" Foreground="#94A3B8" Margin="20,0,8,0"/>
                <TextBox Text="{Binding SearchKeyword}" Width="200" Background="#252536" Foreground="White" BorderBrush="#313244"/>

                <Button Content="导出日志" Command="{Binding ExportLogCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}" Margin="20,0,10,0"/>
                <Button Content="清空日志" Command="{Binding ClearLogCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}"
                        Background="#EF4444" Foreground="White"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="0,15,0,0">
            <DataGrid ItemsSource="{Binding Logs}" 
                      AutoGenerateColumns="False"
                      Background="#1E1E2E"
                      BorderBrush="#313244"
                      RowBackground="#1E1E2E"
                      AlternatingRowBackground="#252536"
                      Foreground="#E2E8F0"
                      BorderThickness="1">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#252536"/>
                            <Setter Property="Foreground" Value="#94A3B8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="120"/>
                    <DataGridTemplateColumn Header="级别" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Level}" 
                                           Foreground="{Binding Level, Converter={StaticResource LogLevelToBrushConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="内容" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>