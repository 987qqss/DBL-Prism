namespace OperationModule.ViewModels
{
    /// <summary>
    /// 设计时数据 - 仅在 Visual Studio 设计器中使用
    /// </summary>
    public class OperateViewModelDesignData : OperateViewModel
    {
        public OperateViewModelDesignData()
        {
            // 这里可以预设一些显示数据
            // 由于 OperateViewModel 目前为空，无需设置
        }
    }
}