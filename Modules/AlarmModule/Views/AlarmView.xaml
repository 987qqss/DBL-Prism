<UserControl x:Class="AlarmModule.Views.AlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="Transparent">

    <UserControl.Resources>
        <!-- 独立窗口不继承 Shell 资源，这里内联 -->
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#94A3B8" />
        <Style x:Key="PanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#1E1E3A" />
            <Setter Property="BorderBrush" Value="#2A2A4A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="12" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部状态栏 -->
        <Border Grid.Row="0" Style="{StaticResource PanelBorderStyle}" Margin="0,0,0,8" Padding="12,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🚨 报警信息" FontSize="14" FontWeight="Bold"
                           Foreground="#EF4444" VerticalAlignment="Center" />
                <TextBlock Text="{Binding StatusText}" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="16,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- 报警列表 -->
        <Border Grid.Row="1" Style="{StaticResource PanelBorderStyle}" Padding="8">
            <DataGrid ItemsSource="{Binding Alarms}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      CanUserReorderColumns="False" CanUserSortColumns="False"
                      Background="Transparent" BorderThickness="0"
                      RowHeaderWidth="0" GridLinesVisibility="None"
                      FontSize="12" RowHeight="32"
                      HeadersVisibility="Column">

                <DataGrid.Resources>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RowBackground}" Value="">
                                <Setter Property="Background" Value="Transparent" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="级别" Width="60" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                        <DataGridTextColumn.Binding>
                            <Binding Path="LevelText" />
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="数据点" Width="*" IsReadOnly="True"
                                        Binding="{Binding PointName}" />
                    <DataGridTextColumn Header="当前值" Width="80" IsReadOnly="True"
                                        Binding="{Binding Value}" />
                    <DataGridTextColumn Header="报警时间" Width="140" IsReadOnly="True"
                                        Binding="{Binding TriggerTime, StringFormat=HH:mm:ss}" />
                    <DataGridTextColumn Header="恢复时间" Width="140" IsReadOnly="True"
                                        Binding="{Binding RecoverTime, StringFormat=HH:mm:ss}" />
                    <DataGridTextColumn Header="状态" Width="80" IsReadOnly="True"
                                        Binding="{Binding StatusText}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRecovered}" Value="True">
                                        <Setter Property="Foreground" Value="#22C55E" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsRecovered}" Value="False">
                                        <Setter Property="Foreground" Value="#EF4444" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
