<UserControl x:Class="DataCollectionModule.Views.DataCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#0F1626">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E2E" BorderBrush="#313244" BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="数据采集监控" FontSize="20" FontWeight="Bold" Foreground="#E2E8F0" Margin="0,0,30,0"/>
                
                <StackPanel Orientation="Horizontal" Margin="0,0,20,0">
                    <Ellipse Width="12" Height="12" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding IsCollecting, StringFormat='采集状态: {0}'}" Foreground="#94A3B8"/>
                </StackPanel>

                <TextBlock Text="频率:" Foreground="#94A3B8" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding CollectFrequency}" Foreground="#FBBF24" FontWeight="Bold"/>

                <Button Content="开始采集" Command="{Binding StartCollectCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}" Margin="20,0,10,0"
                        Background="#22C55E" Foreground="Black"/>
                <Button Content="停止采集" Command="{Binding StopCollectCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}" 
                        Background="#EF4444" Foreground="White"/>
                <Button Content="导出数据" Command="{Binding ExportDataCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="0,15,0,0">
            <DataGrid ItemsSource="{Binding DataPoints}" 
                      AutoGenerateColumns="False"
                      Background="#1E1E2E"
                      BorderBrush="#313244"
                      RowBackground="#1E1E2E"
                      AlternatingRowBackground="#252536"
                      Foreground="#E2E8F0"
                      BorderThickness="1">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#252536"/>
                            <Setter Property="Foreground" Value="#94A3B8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="点位名称" Binding="{Binding Name}" Width="150"/>
                    <DataGridTextColumn Header="数值" Binding="{Binding Value}" Width="100"/>
                    <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="80"/>
                    <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>