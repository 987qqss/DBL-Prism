<UserControl x:Class="StateMachineModule.Views.CommandRunView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:Core.Models;assembly=Core"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 控制按钮栏 -->
        <Border Grid.Row="0"
                Style="{StaticResource PanelBorderStyle}"
                Margin="0,0,0,8"
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="▶ 启动" Command="{Binding StartCommand}"
                            Style="{StaticResource MenuFlatButtonStyle}"
                            Background="#2D8C5A" Foreground="White"
                            Margin="0,0,6,0" ToolTip="按顺序执行队列中所有命令" />
                    <Button Content="⏸ 暂停" Command="{Binding PauseCommand}"
                            Style="{StaticResource MenuFlatButtonStyle}"
                            Background="#D4852A" Foreground="White"
                            Margin="0,0,6,0" ToolTip="暂停当前执行" />
                    <Button Content="⏹ 停止" Command="{Binding StopCommand}"
                            Style="{StaticResource MenuFlatButtonStyle}"
                            Background="#C0392B" Foreground="White"
                            Margin="0,0,6,0" ToolTip="停止并重置" />
                </StackPanel>

                <TextBlock Grid.Column="1"
                           Text="{Binding StatusText}"
                           Foreground="{StaticResource AccentCyanBrush}"
                           FontSize="14" FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="16,0,0,0" />

                <Button Grid.Column="2" Content="🗑 清空队列" Command="{Binding ClearCommand}"
                        Style="{StaticResource MenuFlatButtonStyle}"
                        BorderBrush="{StaticResource BorderBrushDark}" BorderThickness="1"
                        ToolTip="停止并清空队列" />
            </Grid>
        </Border>

        <!-- 队列列表 -->
        <Border Grid.Row="1" Style="{StaticResource PanelBorderStyle}" Padding="8">
            <ListBox ItemsSource="{Binding QueueItems}"
                     Background="Transparent" BorderThickness="0"
                     VirtualizingPanel.ScrollUnit="Pixel">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#1AFFFFFF" CornerRadius="4"
                                Padding="10,6" Margin="0,0,0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="80" />
                                </Grid.ColumnDefinitions>

                                <!-- 序号 -->
                                <TextBlock Grid.Column="0" Text="{Binding Order}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="18" FontWeight="Bold"
                                           VerticalAlignment="Center" HorizontalAlignment="Center" />

                                <!-- 命令名 + 设备名 -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="{StaticResource TextPrimaryBrush}"
                                               FontSize="13" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding DeviceName}"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               FontSize="11" />
                                </StackPanel>

                                <!-- 状态 -->
                                <TextBlock Grid.Column="2" Text="{Binding StatusText}"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           FontSize="12" VerticalAlignment="Center"
                                           HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:QueueItemStatus.Completed}">
                                                    <Setter Property="Foreground" Value="#4CAF50" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:QueueItemStatus.Running}">
                                                    <Setter Property="Foreground" Value="#00D4FF" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:QueueItemStatus.Failed}">
                                                    <Setter Property="Foreground" Value="#EF4444" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="{x:Static models:QueueItemStatus.Paused}">
                                                    <Setter Property="Foreground" Value="#FFB300" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- 结果 -->
                                <TextBlock Grid.Column="3" Text="{Binding Result}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="11" VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis" />

                                <!-- 时间 -->
                                <TextBlock Grid.Column="4" Text="{Binding Time}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="11" FontFamily="Consolas"
                                           VerticalAlignment="Center" HorizontalAlignment="Right" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>
    </Grid>
</UserControl>