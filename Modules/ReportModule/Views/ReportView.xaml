<UserControl x:Class="ReportModule.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#0F1626">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E2E" BorderBrush="#313244" BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="数据报表" FontSize="20" FontWeight="Bold" Foreground="#E2E8F0" Margin="0,0,30,0"/>
                
                <TextBlock Text="时间范围:" Foreground="#94A3B8" Margin="0,0,8,0"/>
                <TextBox Text="{Binding StartDate}" Width="180" Background="#252536" Foreground="White" BorderBrush="#313244" Margin="0,0,10,0"/>
                <TextBlock Text="至" Foreground="#94A3B8" Margin="0,0,10,0"/>
                <TextBox Text="{Binding EndDate}" Width="180" Background="#252536" Foreground="White" BorderBrush="#313244" Margin="0,0,20,0"/>

                <TextBlock Text="报表类型:" Foreground="#94A3B8" Margin="0,0,8,0"/>
                <ComboBox SelectedItem="{Binding ReportType}" Width="100" Background="#252536" Foreground="White" BorderBrush="#313244">
                    <ComboBoxItem>日报</ComboBoxItem>
                    <ComboBoxItem>周报</ComboBoxItem>
                    <ComboBoxItem>月报</ComboBoxItem>
                </ComboBox>

                <Button Content="生成报表" Command="{Binding GenerateReportCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}" Margin="20,0,10,0"/>
                <Button Content="导出报表" Command="{Binding ExportReportCommand}" 
                        Style="{StaticResource MenuFlatButtonStyle}"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="0,15,0,0" Background="#1E1E2E" BorderBrush="#313244" BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <StackPanel Margin="0,0,40,0">
                    <TextBlock Text="最高温度" Foreground="#94A3B8" FontSize="14"/>
                    <TextBlock Text="{Binding MaxTemp}" Foreground="#EF4444" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Margin="0,0,40,0">
                    <TextBlock Text="最低温度" Foreground="#94A3B8" FontSize="14"/>
                    <TextBlock Text="{Binding MinTemp}" Foreground="#60A5FA" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Margin="0,0,40,0">
                    <TextBlock Text="平均温度" Foreground="#94A3B8" FontSize="14"/>
                    <TextBlock Text="{Binding AvgTemp}" Foreground="#FBBF24" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="报警次数" Foreground="#94A3B8" FontSize="14"/>
                    <TextBlock Text="{Binding AlarmCount}" Foreground="#22C55E" FontSize="24" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" Margin="0,15,0,0">
            <DataGrid ItemsSource="{Binding ReportItems}" 
                      AutoGenerateColumns="False"
                      Background="#1E1E2E"
                      BorderBrush="#313244"
                      RowBackground="#1E1E2E"
                      AlternatingRowBackground="#252536"
                      Foreground="#E2E8F0"
                      BorderThickness="1">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#252536"/>
                            <Setter Property="Foreground" Value="#94A3B8"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="100"/>
                    <DataGridTextColumn Header="温度(℃)" Binding="{Binding Temperature}" Width="120"/>
                    <DataGridTextColumn Header="湿度(%)" Binding="{Binding Humidity}" Width="120"/>
                    <DataGridTextColumn Header="电压(V)" Binding="{Binding Voltage}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>